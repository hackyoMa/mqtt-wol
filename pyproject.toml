[project]
name = "mqtt-wol"
version = "2.5.0"
description = "Receive MQTT messages and invoke WOL"
readme = "README.md"
requires-python = "==3.14.5"
dependencies = [
    "paho-mqtt==2.1.0",
    "pywinrm[credssp,kerberos]==0.5.0",
]
